sonar.projectKey=Underwood-Inc_idling.app__UI
sonar.organization=underwood-inc

sonar.inclusions=src/**
sonar.exclusions=e2e/**, **/*.test.tsx, **/*.test.ts, src/lib/test-selectors/**, src/jest.setup.js, src/lib/test-eslint.tsx
# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=idling.app__UI
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

sonar.javascript.lcov.reportPaths=./coverage/lcov.info,coverage/playwright-test-results.json
