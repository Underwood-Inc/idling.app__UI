/**
 * DiceBear Collection Mock - Avatar Styles
 * 
 * This mock covers all the avatar styles that our tests expect from @dicebear/collection
 */

export const lorelei = jest.fn(() => ({
  // Mock style configuration
  schema: {
    properties: {}
  }
}));

export const avataaars = jest.fn(() => ({
  // Mock style configuration
  schema: {
    properties: {}
  }
}));

export const bottts = jest.fn(() => ({
  // Mock style configuration
  schema: {
    properties: {}
  }
}));

export const adventurer = jest.fn(() => ({
  // Mock style configuration
  schema: {
    properties: {}
  }
}));

// Export all available avatar styles for comprehensive mocking
export default {
  lorelei,
  avataaars,
  bottts,
  adventurer
};
