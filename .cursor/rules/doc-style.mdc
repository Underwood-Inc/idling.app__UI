---
alwaysApply: true
---

when writing documents, other than the root codebase README.md or when specified, all documents must be written under the assumption it is intended for docusaurus and so must be available within the documentation site

mermaid charts always user codeblock style (i.e.```mermaid) and never jsx style (i.e. <Mermaid>)
